module Match3 {
    export class Cell extends Phaser.Sprite {
        constructor(game: Phaser.Game, x: number, y: number) {
            super(game, x, y);
            this.init();
        }

        private init(): void {
            
        }
    }
}